'use server';

/**
 * Server Actions for venue/property operations.
 * Placeholder for future server-side venue logic.
 * Currently, venue data is fetched via Edge Functions in lib/property/ and lib/search/.
 */
